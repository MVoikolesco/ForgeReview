# ForgeReview Workflow Studio

The current product is implemented under `backend/` and `frontend/`.
`POC/` is read-only reference material and must not be used as a dependency of
the new application. Use `docs/` for product and technical documentation and
`obsidian/` for durable project memory.
